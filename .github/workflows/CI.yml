name: CI
on:
  push:
    branches: [master]
  pull_request:
    types: [opened, synchronize, reopened]
concurrency:
  # Skip intermediate builds: always.
  # Cancel intermediate builds: only if it is a pull request build.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}
jobs:
  test-windows-1-9:
    name: Test Windows (Julia 1.9)
    runs-on: self-hosted
    steps:
      - name: Initialize instance
        run: |
          Remove-Item -Path '${{ github.workspace }}\*' -Force -Recurse

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Test 1.9
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          SLACK_BOT_USER_OAUTH_ACCESS_TOKEN: ${{ secrets.SLACK_BOT_USER_OAUTH_ACCESS_TOKEN }}
        run: |
          .\test\test_1.9.bat

  test-windows-1-10:
    name: Test Windows (Julia 1.10)
    runs-on: self-hosted
    steps:
      - name: Initialize instance
        run: |
          Remove-Item -Path '${{ github.workspace }}\*' -Force -Recurse

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Test 1.10
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          SLACK_BOT_USER_OAUTH_ACCESS_TOKEN: ${{ secrets.SLACK_BOT_USER_OAUTH_ACCESS_TOKEN }}
        run: |
          .\test\test_1.10.bat

  test-windows-1-11:
    name: Test Windows (Julia 1.11)
    runs-on: self-hosted
    steps:
      - name: Initialize instance
        run: |
          Remove-Item -Path '${{ github.workspace }}\*' -Force -Recurse

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Test 1.11
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          SLACK_BOT_USER_OAUTH_ACCESS_TOKEN: ${{ secrets.SLACK_BOT_USER_OAUTH_ACCESS_TOKEN }}
        run: |
          .\test\test_1.11.bat